repo_download:
    path: /repos/{shortName}/{path}
    controller: App\Controller\RepositoryController::download
    methods: [GET]
    requirements:
        path: '.+'

repo_upload:
    path: /repos/{shortName}/{path}
    controller: App\Controller\RepositoryController::upload
    methods: [PUT, POST]
    requirements:
        path: '.+'